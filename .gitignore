# Terraform state contains sensitive output values — never commit
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/.terraform/
terraform/.terraform.lock.hcl

# AWS credentials — use aws configure or environment variables
.aws/
credentials

# Secrets
.env
*.key
*.pem

# OS noise
.DS_Store
__pycache__/
*.pyc
